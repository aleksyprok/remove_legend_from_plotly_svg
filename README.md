# remove_legend_from_plotly_svg
A lightweight Python utility to automatically remove legends from Plotly-generated SVG files. Useful when you only have the exported SVG and need a clean version without regenerating the figure.

## Installation

uv venv --python 3.14
uv pip install lxml
